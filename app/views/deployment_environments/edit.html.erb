<h2><%= @environment.name %></h2>

<%= form_for @environment, :url => {:controller => 'deployment_environments', :action => 'update', :id => @project, :environment_id => @environment.id}, :html => {:method=>:put} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>
